import { Box, styled } from "@mui/material";

export const StyledNavbar = styled(Box)({
    width: "100%",
    height:"5rem",
    display:"flex",
    alignItems:"center",
    justifyContent: "space-between"
})