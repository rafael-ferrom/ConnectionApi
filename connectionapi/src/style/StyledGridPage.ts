import { Grid, styled } from "@mui/material";

export const StyledGridPage = styled(Grid)({
  width: "80%",
  height: "40rem",
  marginBottom: "2rem",
  textAlign: "center",
  backgroundColor: "gray",
});
